# yagml
A truly game- and platform-agnostic mod loader. Maybe.<br>
This program works by loading mods that are stored as .zip files. These files simply contain files which overwrite existing files.<br>
The program also supports loading multiple mods at once. Conflicts are handled by an ordering system, similar to Minecraft resource packs.
